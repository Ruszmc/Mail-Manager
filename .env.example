# Generate once and keep secret.
# 32-byte urlsafe base64 key. You can generate it with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
MP_FERNET_KEY=PASTE_YOUR_KEY_HERE

MP_DB_URL=sqlite:///./mailpilot.db
